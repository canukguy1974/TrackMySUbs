# This is an example file. Copy this to a new file named .env and fill in your keys.
# The .env file is ignored by git to keep your secrets safe.

# Firebase (Client-side Configuration)
# These are used to initialize the Firebase SDK on the client (in the browser).
# You can find these in your Firebase project settings under "General".
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=

# Firebase (Server-side/Admin SDK Configuration)
# This is for secure backend operations.
# 1. Go to Firebase Console > Project Settings > Service Accounts.
# 2. Generate a new private key, which will download a JSON file.
# 3. Base64-encode the *entire contents* of that JSON file. You can use an online tool or a command like:
#    base64 -w 0 <path/to/your/service-account-file.json> (on Mac/Linux)
# 4. Paste the resulting long string here.
FIREBASE_SERVICE_ACCOUNT_BASE64=

# MongoDB Connection
# Your connection string from MongoDB Atlas or your database provider.
MONGODB_URI=

# Twilio for SMS Notifications
# Get these from your Twilio dashboard.
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# Google AI for Genkit
# Your API key for Gemini from Google AI Studio.
# https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=
